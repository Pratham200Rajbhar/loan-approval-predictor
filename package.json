{
  "name": "loan-approval-predictor",
  "version": "1.0.0",
  "description": "Loan Approval Prediction monorepo for Vercel deployment",
  "scripts": {
    "build": "npm --prefix frontend install && npm --prefix frontend run build"
  },
}
